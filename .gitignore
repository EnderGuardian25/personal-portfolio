node_modules/
.next/
out/
.DS_Store
*.log
.env*.local
.vercel

# Raw photo originals — build-time inputs only, not for production
assets/photography-originals/

# Private documents (full CV with personal details) — never publish
assets/private/

# Claude Code session state — machine-specific
.claude/scheduled_tasks.lock

# Local social posts / exports — machine-specific, not tracked
posts/
